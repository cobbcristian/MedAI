services:
  - type: web
    name: medai-healthcare-platform
    env: static
    buildCommand: cd frontend && npm install && DISABLE_ESLINT_PLUGIN=true npm run build
    staticPublishPath: frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html 